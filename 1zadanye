# Напишите программу, которая принимает на вход цифру, обозначающую день недели,
# и проверяет, является ли этот день выходным.
#
# Пример:
#
# - 6 -> да
# - 7 -> да
# - 1 -> нет

# n=int(input('введите номер дня недели от 1 до 7'))
# if n in range(1,6):
#     print('Будний день')
# else:
#     print('выходной')


# Напишите программу для. проверки истинности утверждения
# ¬(X ⋁ Y ⋁ Z) = ¬X ⋀ ¬Y ⋀ ¬Z для всех значений предикат.

# x=bool
# y=bool
# z=bool
# for x in range(0,2):
#     for y in range(0,2):
#         for z in range(0,2):
#             if not (x or y or z) == (not x or not y or not z):
#                 print(x,y,z,'true')
#             else:
#                 print(x,y,z,'false')


# Напишите программу, которая принимает на вход координаты точки (X и Y),
# причём X ≠ 0 и Y ≠ 0 и выдаёт номер четверти плоскости,
# в которой находится эта точка (или на какой оси она находится).
#
# Пример:
#
# - x=34; y=-30 -> 4
# - x=2; y=4-> 1
# - x=-34; y=-30 -> 3
# x=int(input('введте координату по оси x:'))
# y=int(input('введте координату по оси y:'))
# if x==0 or y==0:
#     print('одна из координат равна 0')
# else:
#     if x>0 and y>0:
#         print ('точка расположена в 1 плоскости')
#     elif x>0 and y<0:
#         print('точка расположена во 2 плоскости')
#     elif x<0 and y<0:
#         print('точка расположена в 3 плоскости')
#     elif x<0 and y>0:
#         print('точка расположена в 4 плоскости')

# Напишите программу, которая по заданному номеру четверти,
# показывает диапазон возможных координат точек в этой четверти (x и y).
n=int(input('введите номер плоскости системы координат от 1 до 4:'))
if n==1:
    print ('Диапазон возможных координат точек: x>0 y>0')
elif n==2:
    print ('Диапазон возможных координат точек: x>0 y<0')
elif n==3:
    print ('Диапазон возможных координат точек: x<0 y<0')
elif n==4:
    print ('Диапазон возможных координат точек: x<0 y>0')

# Напишите программу, которая принимает на вход координаты двух точек и находит расстояние между ними в 2D пространстве.
#
# Пример:
#
# - A (3,6); B (2,1) -> 5,09
# - A (7,-5); B (1,-1) -> 7,21

# ax=int(input('введте координату точки А по оси x:'))
# ay=int(input('введте координату точки А по оси y:'))
# bx=int(input('введте координату точки B по оси x:'))
# by=int(input('введте координату точки B по оси y:'))
# print ('Расстояние между точками равно:', round((((ax-bx)**2)+((ay-by)**2))**0.5,2))

